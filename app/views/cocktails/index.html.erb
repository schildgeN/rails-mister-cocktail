<h1>Cocktails</h1>
<hr>
<br>
<ul class="list-group">
  <% @cocktails.each do |c| %>
    <li class="list-group-item"> <%= c.name %> </li>
    <%= link_to "See details", cocktail_path(c.id), class: "btn btn-primary"%>
    <hr>
  <% end %>
</ul>

<%= link_to "Add a new Cocktail", new_cocktail_path %>
